{
  "parameters": [
    {
      "name": "teamId",
      "description": "ID of the team",
      "in": "path",
      "required": true,
      "schema": {
        "type": "string",
        "format": "uuid"
      }
    },
    {
      "name": "studentId",
      "description": "ID of the invitee",
      "in": "path",
      "required": true,
      "schema": {
        "type": "string",
        "format": "uuid"
      }
    }
  ],
  "post": {
    "tags": ["teams"],
    "operationId": "invite_to_team",
    "summary": "Invite another student to team",
    "responses": {
      "204": {
        "description": "Invite sent successfully"
      },
      "4XX": {
        "$ref": "../../responses/ClientErrorResponse.json"
      }
    }
  }
}
