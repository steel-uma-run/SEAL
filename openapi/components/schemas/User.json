{
  "description": "A user account",
  "type": "object",
  "required": ["id", "email", "role", "name"],
  "properties": {
    "id": {
      "type": "string",
      "format": "uuid"
    },
    "email": {
      "type": "string",
      "format": "email"
    },
    "role": {
      "$ref": "./Role.json"
    },
    "name": {
      "type": "string"
    }
  }
}
