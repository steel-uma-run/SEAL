{
  "type": "object",
  "required": ["id", "submission_id", "lecturer_id", "judge_score", "average_score", "is_resolved", "status", "judge_reason", "created_at"],
  "properties": {
    "id": {
      "type": "string",
      "format": "uuid"
    },
    "submission_id": {
      "type": "string",
      "format": "uuid"
    },
    "criteria_id": {
      "type": "string",
      "format": "uuid"
    },
    "lecturer_id": {
      "type": "string",
      "format": "uuid"
    },
    "judge_score": {
      "type": "number",
      "format": "double"
    },
    "average_score": {
      "type": "number",
      "format": "double"
    },
    "is_resolved": {
      "type": "boolean"
    },
    "status": {
      "type": "string",
      "enum": [
        "PENDING",
        "ACCEPTED",
        "REJECTED"
      ],
      "description": "Status of the deviation notification"
    },
    "judge_reason": {
      "type": "string",
      "description": "Reason provided by judge if they reject the deviation"
    },
    "created_at": {
      "type": "string",
      "format": "date-time"
    }
  }
}